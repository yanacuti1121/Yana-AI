[build]
builder = "DOCKERFILE"
dockerfilePath = "tools/yana-web/Dockerfile"

[deploy]
startCommand = "node tools/yana-web/server.js"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
