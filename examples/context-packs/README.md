# Context-Pack Examples (Phase 1B.1)

These examples are reference fixtures for a future `check-context` validator.

- `valid-basic/` is intended to **pass** future validation.
- `invalid-broad-scope/` is intentionally weak and should **fail** future validation.

This folder is examples-only and does not implement runtime enforcement.
